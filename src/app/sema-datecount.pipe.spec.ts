import { SemaDatecountPipe } from './sema-datecount.pipe';

describe('SemaDatecountPipe', () => {
  it('create an instance', () => {
    const pipe = new SemaDatecountPipe();
    expect(pipe).toBeTruthy();
  });
});
